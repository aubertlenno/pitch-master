l_logo = Image.open('/mnt/data/pl_logo.png')  # Ensure you have the correct path to the image
        # pl_logo = pl_logo.resize((100, 100), Image.ANTIALIAS)  # Resize the image if needed and preserve aspect ratio
        # pl_logo = ImageTk.PhotoImage(pl_logo)

        # pl_logo_label = Label(self.window, image=pl_logo, bg='#38003c')
        # pl_logo_label.image = pl_logo  # Keep a reference to avoid garbage collection
        # pl_logo_label.place(x=320, y=480)
